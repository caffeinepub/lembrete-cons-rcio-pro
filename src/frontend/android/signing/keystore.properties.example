# Android Signing Configuration Template
# Copy this file to keystore.properties (which is gitignored) and fill in your values
# NEVER commit the actual keystore.properties file with real credentials

# Path to your release keystore file (relative to android/ directory)
storeFile=./signing/release.keystore

# Keystore password
storePassword=YOUR_KEYSTORE_PASSWORD_HERE

# Key alias
keyAlias=consorcio-pro-key

# Key password
keyPassword=YOUR_KEY_PASSWORD_HERE

# Instructions:
# 1. Generate a keystore if you don't have one:
#    keytool -genkey -v -keystore signing/release.keystore -alias consorcio-pro-key -keyalg RSA -keysize 2048 -validity 10000
#
# 2. Get the SHA-256 fingerprint for Digital Asset Links:
#    keytool -list -v -keystore signing/release.keystore -alias consorcio-pro-key
#
# 3. Copy the SHA-256 fingerprint to frontend/public/.well-known/assetlinks.json
#
# 4. Copy this file to keystore.properties and fill in your actual passwords
